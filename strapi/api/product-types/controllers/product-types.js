'use strict';
const { sanitizeEntity } = require('strapi-utils');

/**
 * Read the documentation (https://strapi.io/documentation/developer-docs/latest/development/backend-customization.html#core-controllers)
 * to customize this controller
 */

module.exports = {
	// async find(ctx) {
	    // const populate = ['cases', 'cases.fabrics', 'fabrics']
// 
	    // const entities = await strapi.services['product-types'].find(ctx.query, populate)
// 
	    // console.log('e',entities[1].cases)
// 
	    // return entities.map(entity => (sanitizeEntity(entity, { model: strapi.models['product-types'] })))
	  // },
};
